# Completed Work Archive

The completed work that used to live here has been consolidated into:

- `CHANGELOG.md` for release-facing summaries.
- `history.md` for chronological one-line entries.
- `completion_log/` for verification commands and pushed commit references.

Keep this directory lightweight. If a completed task body needs to remain
available briefly, summarize or move its durable context into the files above
before the next release cleanup.
