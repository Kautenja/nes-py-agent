# nes-py Mapper 000 NROM Coverage

## Summary

- Added representative-title Python mapper coverage for `Super Mario Bros. (USA)` at `nes_py/tests/games/super-mario-bros-1.nes`.
- The representative test verifies mapper 0 header metadata, constructs `NESEnv`, runs reset, deterministic steps, `rgb_array` render, and retained package backup/restore workflow.
- Added native Catch2 emulator save-state coverage proving mapper-owned NROM PRG RAM survives `Emulator::backup()` / `restore()`.
- Kept existing native mapper 0 characterization coverage for fixed PRG mapping, 16 KiB PRG mirroring, CHR ROM reads, and nametable mirroring.

## Verification

- `.venv/bin/python -m pip install -e .` (passed)
- `cmake -S . -B build/nes-emu-debug -DCMAKE_BUILD_TYPE=Debug -DNES_EMU_BUILD_TESTS=ON -DNES_EMU_BUILD_BENCHMARKS=OFF` (passed)
- `cmake --build build/nes-emu-debug --config Debug --target nes_emu_tests` (passed)
- `./build/nes-emu-debug/nes_emu_tests "[mapper][nrom]"` (21 assertions in 3 test cases passed)
- `ctest --test-dir build/nes-emu-debug -C Debug --output-on-failure` (26 tests passed)
- `.venv/bin/python -m unittest nes_py.tests.mappers.test_mapper_000_nrom` (3 tests passed)
- `.venv/bin/python -m unittest discover nes_py/tests/mappers` (12 tests passed)
- `.venv/bin/python -m unittest nes_py.tests.test_rom.ShouldReadSuperMarioBros` (21 tests passed)
- `.venv/bin/python -m unittest nes_py.tests.test_nes_env` (23 tests passed)
- `git -C nes-py diff --check` (passed)

Notes:

- The local Super Mario Bros. fixture was present, so the representative ROM-backed integration test ran and passed. No ROM downloads or new ROM assets were added.
- Native benchmarks were not run because this work item changed tests only, not mapper timing hooks or hot-path behavior.

## Commits

- `nes-py` child submodule: `10391261e6565d427f01fda628ddcc908d5afec4`, pushed to `origin/ralph-dev`.
- Umbrella repository: this completion commit records the `nes-py` gitlink update, archive summary, history entry, and completion log.
