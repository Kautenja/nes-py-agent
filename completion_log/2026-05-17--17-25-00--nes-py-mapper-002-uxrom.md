# nes-py Mapper 002 UxROM Coverage

## Summary

- Added representative-title Python mapper coverage for `Mega Man (USA)` at `nes_py/tests/games/mega-man.nes`.
- The representative test verifies mapper 2 header metadata, constructs `NESEnv`, runs reset, deterministic steps, `rgb_array` render, and retained package backup/restore workflow.
- Added native Catch2 emulator save-state coverage proving the selected UxROM PRG bank and CHR RAM contents survive `Emulator::backup()` / `restore()`.
- Kept existing native mapper 2 characterization coverage for switchable PRG banking, fixed final PRG bank behavior, CHR RAM reads/writes, and safe bank masking.

## Verification

- `.venv/bin/python -m pip install -e .` (passed)
- `cmake -S . -B build/nes-emu-debug -DCMAKE_BUILD_TYPE=Debug -DNES_EMU_BUILD_TESTS=ON -DNES_EMU_BUILD_BENCHMARKS=OFF` (passed)
- `cmake --build build/nes-emu-debug --config Debug --target nes_emu_tests` (passed)
- `./build/nes-emu-debug/nes_emu_tests "[mapper][uxrom]"` (16 assertions in 2 test cases passed)
- `ctest --test-dir build/nes-emu-debug -C Debug --output-on-failure` (26 tests passed)
- `.venv/bin/python -m unittest nes_py.tests.mappers.test_mapper_002_uxrom` (2 tests passed)
- `.venv/bin/python -m unittest discover nes_py/tests/mappers` (12 tests passed)
- `git -C nes-py diff --check` (passed)

Notes:

- The local Mega Man fixture was present, so the representative ROM-backed integration test ran and passed. No ROM downloads or new ROM assets were added.
- Native benchmarks were not run because this work item changed tests only, not mapper timing hooks or hot-path behavior.

## Commits

- `nes-py` child submodule: `10391261e6565d427f01fda628ddcc908d5afec4`, pushed to `origin/ralph-dev`.
- Umbrella repository: this completion commit records the `nes-py` gitlink update, archive summary, history entry, and completion log.
