# nes-py Mapper 004 MMC3

Completed work item 034 by adding a native MMC3 mapper implementation and mapper 4
factory/registry support in `nes-py`.

## Summary

- Added MMC3 PRG/CHR bank mode handling, mapper-controlled mirroring, PRG RAM
  enable/write-protect behavior, filtered PPU A12 IRQ counter behavior, and
  cloneable mapper state.
- Added dedicated native mapper 004 Catch2 coverage for PRG banking, CHR
  banking, mirroring, PRG RAM protection, IRQ reload/filter behavior, and
  save-state cloning.
- Added public Python mapper 004 smoke coverage plus the local
  `nes_py/tests/games/super-mario-bros-3.nes` representative-title workflow.
- Moved existing mapper 4 on-disk fixtures out of the unsupported constructor
  test because mapper 4 is now supported.

## Verification

Commands run in `/private/tmp/nes-py-spec-034`:

- `/private/tmp/nes-py-spec-034-venv/bin/python -m pip install -e .` passed
  after allowing dependency downloads for the throwaway venv.
- `cmake -S . -B build/nes-emu-debug -DCMAKE_BUILD_TYPE=Debug -DNES_EMU_BUILD_TESTS=ON -DNES_EMU_BUILD_BENCHMARKS=OFF` passed after allowing Catch2 fetch.
- `cmake --build build/nes-emu-debug --config Debug --target nes_emu_tests` passed.
- `build/nes-emu-debug/nes_emu_tests "[mapper][mmc3]"` passed: 62 assertions in 7 test cases.
- `ctest --test-dir build/nes-emu-debug -C Debug --output-on-failure` passed: 33/33 tests.
- `/private/tmp/nes-py-spec-034-venv/bin/python -m unittest discover nes_py/tests/mappers` passed: 14 tests.
- `/private/tmp/nes-py-spec-034-venv/bin/python -m unittest discover nes_py/tests` passed: 192 tests.
- `cmake -S . -B build/nes-emu-bench -DCMAKE_BUILD_TYPE=Release -DNES_EMU_BUILD_TESTS=OFF -DNES_EMU_BUILD_BENCHMARKS=ON` passed after allowing Catch2 fetch.
- `cmake --build build/nes-emu-bench --config Release --target nes_emu_benchmarks` passed.
- `build/nes-emu-bench/nes_emu_benchmarks "[benchmark]"` passed.

The representative SMB3 fixture was present locally, so the ROM-backed Python
mapper 004 integration test ran instead of skipping.

## Commits

- `nes-py`: `4cf7f03446f45a61a3335b4c73935bdf187cd0e3` (`codex/spec-034-mmc3`) pushed to origin.
